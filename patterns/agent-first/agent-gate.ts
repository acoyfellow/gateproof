#!/usr/bin/env bun
/**
 * Agent Gate Pattern — Full integration example
 *
 * This file is intentionally not simulated. Wire it to a real FilepathRuntime
 * and a live agent container before you run it.
 *
 * Run: bun run patterns/agent-first/agent-gate.ts
 */

throw new Error(
  "This pattern will not fake agent output. Register a real FilepathRuntime and a real observe source in your app before running it."
);
