# gateproof demo

This demo app is a userland consumer of the root `plan.ts`.

It renders:

- the homepage hero
- the `/docs` pages

Both are expected to match the root `README.md`, which is generated from the same source.
