# gateproof

Gateproof is a tiny Effect-first runtime.

The root `plan.ts` is the handoff file.
The root `README.md` is generated from that source.
This demo app is only a presentation layer over the same truth.
